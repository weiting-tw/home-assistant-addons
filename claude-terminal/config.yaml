---
name: "Claude Terminal"
description: "Terminal interface for Anthropic's Claude Code CLI with persistent auth and packages"
version: "1.10.0"
slug: "claude_terminal"
image: "ghcr.io/weiting-tw/claude-terminal-{arch}"
init: false

# Supported architectures
arch:
  - aarch64
  - amd64
  - armv7

# External resources
url: "https://github.com/anthropics/claude-code"

# Web interface configuration
ingress: true
ingress_port: 7681
panel_icon: mdi:code-braces
panel_title: "Claude Terminal"
panel_admin: true

# Port configuration
ports:
  7681/tcp: 7681
ports_description:
  7681/tcp: "Web terminal (not required for ingress)"

# Add-on configuration options
options:
  auto_launch_claude: true
  system_prompt: "你是一個運行在 Home Assistant 的 AI 助理。請永遠使用繁體中文回應，除非使用者明確要求其他語言。"
  persistent_apk_packages: []
  persistent_pip_packages: []
schema:
  auto_launch_claude: bool?
  system_prompt: str?
  persistent_apk_packages:
    - str
  persistent_pip_packages:
    - str

# Security: no config mount (secrets.yaml not exposed)
# map: []

# API access — minimal privileges
hassio_api: true
hassio_role: default
homeassistant_api: false
auth_api: false

# Service startup configuration
startup: services

# Web UI access URL
webui: http://[HOST]:[PORT:7681]/
